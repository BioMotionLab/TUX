using UnityEngine;

namespace BML_Utilities {
    public class Rotator : MonoBehaviour {

        public float speed;
        public int   frameCount = 0;

    
        // Update is called once per frame
        void Update() {
            frameCount++;
            Debug.Log("FEFofjnasdlfnsad;l");
        }
    }
}
